# Azure Service Bus Emulator — started via docker-compose.yml
servicebus.connection-string=Endpoint=sb://localhost;SharedAccessKeyName=RootManageSharedAccessKey;SharedAccessKey=SAS_KEY_VALUE;UseDevelopmentEmulator=true;

camel.component.azure-servicebus.connection-string=${servicebus.connection-string}

# Speed up delays in tests
service-bus-route.step-delay-ms=1000
